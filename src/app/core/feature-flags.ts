/**
 * Temporary product switches.
 *
 * Keep the UI/service implementation in place, but run project chats in
 * advice-only mode for now.
 */
export const PROJECT_FILE_APPLY_ENABLED = false;
export const PROJECT_PROPOSED_CHANGES_ENABLED = false;
